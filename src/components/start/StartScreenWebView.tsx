import styled from "styled-components/native";
import * as Constants from "../../constants/Constants";

const StartScreenWebView = styled.View`
  flex: 1;
  background-color: white;
`;

export default StartScreenWebView;
